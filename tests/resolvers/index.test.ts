// import { request } from '../../src/algorithm';
// import { Context } from '../../src/context';
// import { resolvers } from '../../src/resolvers/index';
// import { Company, GenerateScheduleInput, Term } from '../../src/schema';

describe('Test generate schedule mutation functionality', async () => {
  it('Should return not logged in error if not logged in', async () => {
    // Given
    /* const _: GenerateScheduleInput = {
      algorithm1: Company.Company3,
      algorithm2: Company.Company3,
      courses: [
        {
          code: '426',
          section: 1,
          subject: 'SENG',
        },
      ],
      term: Term.Fall,
      year: 2022,
    };*/
    /* const context: Context = {
      algorithm: request,
    };*/
    // When
    // const response = resolvers.Mutation?.generateSchedule?( input, context);
    // Then
  });

  it('Should return not logged in error if not logged in', async () => {
    // Given
    // When
    // Then
  });
});
